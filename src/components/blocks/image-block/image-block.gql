fragment ImageBlock on ImageBlockRecord {
  id
  image {
    responsiveImage(imgixParams: { auto: format, w: 690 }) {
      srcSet
      webpSrcSet
      sizes
      src
      width
      height
      aspectRatio
      alt
      title
      base64
    }
  }
}
